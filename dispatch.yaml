
# on supplante les règles des routages

dispatch:
  # pour tous les noms de domaine suivi de admin/ <n'import quoi> c'est le service admin
  - url: "/admin/*"
    service: admin

  - url: "/front/*"
    service: front

  - url: "/login/*"
    service: auth

